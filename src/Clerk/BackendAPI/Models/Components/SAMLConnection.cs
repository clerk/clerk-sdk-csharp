//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Clerk.BackendAPI.Models.Components
{
    using Clerk.BackendAPI.Models.Components;
    using Clerk.BackendAPI.Utils;
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class SAMLConnectionType
    {
        private SAMLConnectionType(string value) { Value = value; }

        public string Value { get; private set; }

        public static SAMLConnectionType One { get { return new SAMLConnectionType("1"); } }

        public static SAMLConnectionType Two { get { return new SAMLConnectionType("2"); } }

        public override string ToString() { return Value; }
        public static implicit operator String(SAMLConnectionType v) { return v.Value; }
        public static SAMLConnectionType FromString(string v) {
            switch(v) {
                case "1": return One;
                case "2": return Two;
                default: throw new ArgumentException("Invalid value for SAMLConnectionType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((SAMLConnectionType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }

    [JsonConverter(typeof(SAMLConnection.SAMLConnectionConverter))]
    public class SAMLConnection
    {
        public SAMLConnection(SAMLConnectionType type)
        {
            Type = type;
        }

        [SpeakeasyMetadata("form:explode=true")]
        public Models.Components.One? One { get; set; }

        [SpeakeasyMetadata("form:explode=true")]
        public Models.Components.Two? Two { get; set; }

        public SAMLConnectionType Type { get; set; }
        public static SAMLConnection CreateOne(Models.Components.One one)
        {
            SAMLConnectionType typ = SAMLConnectionType.One;

            SAMLConnection res = new SAMLConnection(typ);
            res.One = one;
            return res;
        }
        public static SAMLConnection CreateTwo(Models.Components.Two two)
        {
            SAMLConnectionType typ = SAMLConnectionType.Two;

            SAMLConnection res = new SAMLConnection(typ);
            res.Two = two;
            return res;
        }

        public class SAMLConnectionConverter : JsonConverter
        {
            public override bool CanConvert(System.Type objectType) => objectType == typeof(SAMLConnection);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                if (reader.TokenType == JsonToken.Null)
                {
                    throw new InvalidOperationException("Received unexpected null JSON value");
                }

                var json = JRaw.Create(reader).ToString();
                var fallbackCandidates = new List<(System.Type, object, string)>();

                try
                {
                    return new SAMLConnection(SAMLConnectionType.One)
                    {
                        One = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<Models.Components.One>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(Models.Components.One), new SAMLConnection(SAMLConnectionType.One), "One"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                try
                {
                    return new SAMLConnection(SAMLConnectionType.Two)
                    {
                        Two = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<Models.Components.Two>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(Models.Components.Two), new SAMLConnection(SAMLConnectionType.Two), "Two"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                if (fallbackCandidates.Count > 0)
                {
                    fallbackCandidates.Sort((a, b) => ResponseBodyDeserializer.CompareFallbackCandidates(a.Item1, b.Item1, json));
                    foreach(var (deserializationType, returnObject, propertyName) in fallbackCandidates)
                    {
                        try
                        {
                            return ResponseBodyDeserializer.DeserializeUndiscriminatedUnionFallback(deserializationType, returnObject, propertyName, json);
                        }
                        catch (ResponseBodyDeserializer.DeserializationException)
                        {
                            // try next fallback option
                        }
                        catch (Exception)
                        {
                            throw;
                        }
                    }
                }

                throw new InvalidOperationException("Could not deserialize into any supported types.");
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null)
                {
                    throw new InvalidOperationException("Unexpected null JSON value.");
                }

                SAMLConnection res = (SAMLConnection)value;

                if (res.One != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.One));
                    return;
                }

                if (res.Two != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Two));
                    return;
                }
            }

        }

    }
}